/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BceidUserData } from '../../models/bceid-user-data';

export interface ContactGetLoginInfo$Params {
}

export function contactGetLoginInfo(http: HttpClient, rootUrl: string, params?: ContactGetLoginInfo$Params, context?: HttpContext): Observable<StrictHttpResponse<BceidUserData>> {
  const rb = new RequestBuilder(rootUrl, contactGetLoginInfo.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BceidUserData>;
    })
  );
}

contactGetLoginInfo.PATH = '/api/contacts/getlogin';
