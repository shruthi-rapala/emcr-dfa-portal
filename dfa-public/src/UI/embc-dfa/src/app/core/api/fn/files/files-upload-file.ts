/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApplicationResult } from '../../models/application-result';

export interface FilesUploadFile$Params {
  id: string;
  'file-classification'?: string | null;
  'file-tag'?: string | null;
  'file-folder'?: string | null;
      body?: {
'File'?: Blob | null;
}
}

export function filesUploadFile(http: HttpClient, rootUrl: string, params: FilesUploadFile$Params, context?: HttpContext): Observable<StrictHttpResponse<ApplicationResult>> {
  const rb = new RequestBuilder(rootUrl, filesUploadFile.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('file-classification', params['file-classification'], {});
    rb.header('file-tag', params['file-tag'], {});
    rb.header('file-folder', params['file-folder'], {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApplicationResult>;
    })
  );
}

filesUploadFile.PATH = '/api/Files/{id}';
