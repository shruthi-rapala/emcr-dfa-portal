/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface FilesDownloadFile$Params {
  id: string;
  'file-folder'?: string | null;
}

export function filesDownloadFile(http: HttpClient, rootUrl: string, params: FilesDownloadFile$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
  const rb = new RequestBuilder(rootUrl, filesDownloadFile.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('file-folder', params['file-folder'], {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/octet-stream', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Blob>;
    })
  );
}

filesDownloadFile.PATH = '/api/Files/{id}';
