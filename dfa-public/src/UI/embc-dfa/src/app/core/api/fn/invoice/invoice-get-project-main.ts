/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DfaClaimMain } from '../../models/dfa-claim-main';

export interface InvoiceGetProjectMain$Params {

/**
 * The project Id.
 */
  projectId?: string;
}

export function invoiceGetProjectMain(http: HttpClient, rootUrl: string, params?: InvoiceGetProjectMain$Params, context?: HttpContext): Observable<StrictHttpResponse<DfaClaimMain>> {
  const rb = new RequestBuilder(rootUrl, invoiceGetProjectMain.PATH, 'get');
  if (params) {
    rb.query('projectId', params.projectId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DfaClaimMain>;
    })
  );
}

invoiceGetProjectMain.PATH = '/api/invoices/appmain/byId';
