import { CurrentApplication } from '../../core/api/models';

export class DashboardMappingService {
  constructor(
    
  ) { }

  mapCurrentApplications(profile: CurrentApplication): void {
    
  }
}
